<% if !@agreement.errors.empty? %>
   <div class = "alert alert-error">
      <ul>
         <% @agreement.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
         <% end %>
      </ul>
   </div>
<% end %>

<div class = "well">
  <h3>Send to DocuSign via Template</h3>
   <%= form_for @agreement, url: '/template', html: { multipart: true } do |f| %>
      <%= f.label :names %>
      <%= f.text_field :names %>
      <br />

      <%= f.label :emails %>
      <%= f.text_field :emails %>
      <span>Please seperate emails by commas :)</span>
      <br />

      <%= f.submit "Save", class: "btn btn-primary" %>
   <% end %>
  <%= link_to "Agreements Home", '/agreements', class: 'action-button margin-top' %>
</div>
